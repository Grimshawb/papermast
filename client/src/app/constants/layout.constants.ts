/**
 * The viewport width below which the app switches to its mobile presentation:
 * the toolbar collapses into the side nav and the header search box is replaced
 * by per-page forms. Matches the Bootstrap `md` breakpoint the SCSS uses.
 */
export const MOBILE_BREAKPOINT = '(max-width: 767.98px)';
