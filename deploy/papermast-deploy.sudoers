# The deployment account has no administrative access beyond this validated script.
# deploy.sh accepts exactly one 40-character lowercase hexadecimal commit SHA.
deploy ALL=(root) NOPASSWD: /usr/local/sbin/papermast-deploy *
